import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  square: {
    borderColor: "#2b4f85",
    borderWidth: 1,
    width: 75,
    height: 75,
    justifyContent: "center",
    alignItems: "center",
  },
  text: {
    color: "#2b4f85",
    fontSize: 22,
    fontWeight: "bold",
  },
});